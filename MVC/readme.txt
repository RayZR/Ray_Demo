Steps to setup this demo site:
1.import MVC_backup to database;
2.configure database and host info in config.php



=====================================================================
The login for admin site: login:ray password:ray.
Password is stored in database with sha256 encryption and hashed with salt.

There are some simple demos of uploading files, delete, insert,update database.
